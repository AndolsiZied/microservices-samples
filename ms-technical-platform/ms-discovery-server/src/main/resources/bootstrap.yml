spring:
  application:
    name : discovery-server

server:
  port: 9191

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
  server:
    waitTimeInMsWhenSyncEmpty: 0
  serviceUrl:
      defaultZone: http://discovery-server:9191