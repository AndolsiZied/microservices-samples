version: '3'
services:
  mongo:
    image: mongo:3.4.10
    environment:
      - MONGO_INITDB_ROOT_USERNAME:ms-username-dev
      - MONGO_INITDB_ROOT_PASSWORD:ms-password-dev
    ports:
      - "27017:27017"
    volumes:
      - /mnt/data/mongo:/data/db
    command: mongod
  configserver:
    image: zandolsi/ms-config-server:fs-config
    ports:
      - "9090:9090"

  establishmentservice:
    image: zandolsi/ms-establishment:external-config
    ports:
      - "8080:8080"
    depends_on:
      - mongo
      - configserver