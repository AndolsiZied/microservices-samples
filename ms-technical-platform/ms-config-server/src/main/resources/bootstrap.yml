spring:
  application:
    name : configuration-server
  cloud:
    config:
      server:
        encrypt.enabled: false
        git:
          uri: https://github.com/AndolsiZied/configuration-repo/
          searchPaths: establishment-services,user-services,booking-services
          timeout: 30